<div class="banner mb-3" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://t2.tudocdn.net/639831?w=1920);">
  <div class="container">
    <h1>Save <strong>any</strong> kind of <strong> movies</strong>!</h1>
    <p><span data-controller="typed-js"></span></p>
  </div>
</div>

<%= link_to "Add a new list", new_list_path, class: "btn btn-primary float-end" %>


<div class="container">
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list), class: "btn "  %></li>
    <% end %>
  </ul>
</div>
